@using HRM.DTOs.Leave
@using HRM.Services.State
@using System.ComponentModel.DataAnnotations
@using Microsoft.EntityFrameworkCore;

<div class="bg-white rounded-lg border border-slate-200 p-6">

    <h3 class="text-lg font-semibold mb-4">
        Sick Leave Information
    </h3>

    <div class="mb-4">
        <label>Medical Certificate No</label>

  @*       <InputText @bind-Value="Model.MedicalCertificateNo"
                   class="w-full h-11 px-3 border rounded-md" />
    </div>

    <div class="mb-4">
        <label>Hospital Name</label>

        <InputText @bind-Value="Model.HospitalName"
                   class="w-full h-11 px-3 border rounded-md" />
    </div>

    <div>
        <label>Reason</label>

        <InputSelect @bind-Value="Model.LeaveReasonId"
                     class="w-full h-11 px-3 border rounded-md">

            <option value="0">-- Select Reason --</option>

            @foreach (var item in Reasons)
            {
                <option value="@item.Id">@item.Name</option>
            }

        </InputSelect> *@
    </div>

</div>

@code {
    [Parameter]
    public LeaveApplicationState LeaveState { get; set; } = default!;
}